# frontend 현재 상태

> **덮어쓰기 스냅샷.** 매 턴 종료 전에 이 파일 전체를 최신 상태로 갱신한다(헌법 제4조 1항).
> 시간순 기록은 `WORKLOG.md`에 append 한다.

마지막 갱신: 2026-07-29 23:45 (API 명세·요구사항 정의서 2026-07-29 변경 사항 반영)

## 현재 단계

**계약 변경 전파 대기** — 2026-07-29 API 명세·요구사항 정의서 업데이트를 프론트엔드 관점에서 통합. 갭 18건으로 재분류 완료(기존 12 + 신규 6). 화면 설계 가능한 경계가 명확해짐. **코드 없음.**

## 진행 중 작업

1. 갭 재분류 및 새 UI 요구사항 도출 — **완료** (2026-07-29)
   - 기존 12건 재검토: 모두 잔존 (0개 해소)
   - 신규 6건 발견 (REQUEST_CHANGES, 한국어+원문, PAUSED_QUOTA, 용어 병기, 언어 표시, 보류 알림)
   - 근거: API 명세 8.1절·2.7절·2.8절, 요구사항 정의서 5.7절·5.12절·5.15절

## 다음 작업

1. **원본 언어와 무관하게 한국어 위키 본문 + 원문 발췌 혼합 화면 설계** — 갭 #14
   - Markdown 렌더링에서 발췌 구간 시각적 표기 방안 (언어 배지, 회색 배경 등)
   - 검색 하이라이트 처리 (원문 구간 포함 인덱싱)
   - 근거: UD-24(위키 한국어), FR-UIX-12·13·14

2. **변경 검토 화면 4개 판단 버튼 + REQUEST_CHANGES 수정 요청 흐름** — 갭 #13
   - 기존 3개 → 4개 버튼 (REQUEST_CHANGES 추가)
   - comment 필수/선택 필드 규칙
   - 재생성 결과 대기 UX
   - 근거: FR-CHG-05, API 명세 8.1절

3. **비동기 작업 polling 간격 권고안** — 갭 #9
   - Ingest(진행도 있음): 2~3초
   - AI 답변(생성 중): 1초 또는 즉시
   - Lint·Export: 2~5초
   - 근거: UD-04(미확정), 요구사항 정의서 3절 UX 검증

4. **처리량 보류(PAUSED_QUOTA) vs 실패(FAILED) 시각 구분** — 갭 #15
   - PAUSED_QUOTA: "보류 중, 구매 후 재개 가능"
   - FAILED: "종결, 재시도 필요"
   - 알림 통합 (FR-NTF-05)
   - 근거: FR-ING-26, FR-NTF-05

5. **원본 감지 언어 표시** — 갭 #17
   - 원본 목록에서 언어 배지 (예: "영어", "한국어")
   - 근거: API 5.1절 detectedLanguage

6. **원문 용어 병기(첫 등장 1회)의 검색·렌더링 전략** — 갭 #16
   - 전문 검색에 괄호 안 용어 포함 인덱싱
   - markdown 렌더링 스타일 (글꼴, 강조 방식)
   - diff 뷰에서 병기 추가/제거가 내용 변경으로 오인되지 않도록
   - 근거: UD-27(확정), FR-UIX-14

7. **기존 12개 갭에 대한 구현 차단 요인 명시** — 다음 단계 프로젝트
   - 갭 #1, #2, #3: backend·리더 담당 (글로벌 홈·도서관 홈 구현 차단)
   - 갭 #4, #5, #6, #11, #12: backend 담당 (API 설계)
   - 갭 #7: frontend 권고 필요 (설계)
   - 갭 #8, #9: 미확정 계약(UD-05, UD-04) 확정 필요
   - 갭 #10: MVP 범위 확정 (Phase 2 SSE)

## 차단 요인

**프론트엔드 화면 구현 직접 차단:**
1. **UD-04 (polling 간격)** — 비동기 작업 진행도 표시 UX의 구체적 timing
2. **UD-05 (diff 표현)** — diff 뷰어 구현 방식 (unifed vs 블록)
3. **갭 #2 (도서관 홈 API)** — 도서관 홈 스키마 불명확 → API 명확화 필요

**설계 결정 필요:**
- 원문 발췌 시각 표기 방식 (언어 배지·배경색·글꼴)
- 원문 용어 병기 렌더링 스타일
- polling 간격별 UX (Ingest 단계별 다른 속도)

## 소유 산출물

| 경로 | 상태 | 갱신 |
| --- | --- | --- |
| `docs/검증-프론트엔드-화면-API-갭.md` | **재분류 완료** (기존 12 + 신규 6 = 총 18건) | 2026-07-29 |
| `frontend/` | 미생성 (설계 선행 중) | — |
| 화면 와이어프레임 | 미작성 (갭 #13, #14, #15 우선) | — |
