# frontend 현재 상태

> **덮어쓰기 스냅샷.** 매 턴 종료 전에 이 파일 전체를 최신 상태로 갱신한다(헌법 제4조 1항).
> 시간순 기록은 `WORKLOG.md`에 append 한다.

마지막 갱신: 2026-07-30 (도서관 홈 화면 설계 완료, 갭 #21 발견, 라이브러리 승인 대기)

## 현재 단계

**도서관 홈 화면 설계 완료** — UD-28 확정(2026-07-29)으로 갭 #2·#3이 해소됨. 사용자 새 확정 5건(UD-01·UD-02·UD-06·UD-07·UD-14) 반영으로 갭 #19·#20 신규 발견. 도서관 홈 설계 중 갭 #21 발견(모순 조회 API 부재). 라이브러리 선정 대기. **목록 형태 와이어프레임 + 설계 결정 근거 완료. 목업 코드 미작성.**

## 진행 중 작업

1. UD-04·UD-05 확정 상태 검증 및 갭 문서 갱신 — **완료** (2026-07-29)
   - API 명세 18절 1490~1493행에 두 항목 확정 내용 명시
   - 요구사항 정의서 12절 687~688행에 취소선 처리됨
   - 갭 문서: 갭 #8·#9 "해소됨"으로 표기, 근거 절 번호 명시
   - 근거: frontend 권고 채택(2026-07-29, 리더)

2. UD-28·UD-01·UD-02 확정 사항 반영 — **완료** (2026-07-30)
   - 갭 #2·#3: "형태 변경" → "해소됨" 정정 (UD-28 확정 내용: CONCEPT 페이지, 관계 수 양방향 합산, 상위 6개, PUBLISHED만, 변경 세트 apply 후 비동기 재계산)
   - 갭 #19 신규 추가: 업로드 한도 검증 및 413 에러 (UD-01: 파일 200MB, ZIP 1GB, 문서 1000개)
   - 갭 #20 신규 추가: 토큰 만료 세션 재발급 (UD-02: Access 15분, Refresh 30일 → polling 중 401 처리)
   - UD-06·UD-07·UD-14는 기존 갭 #13·#14에 이미 반영됨
   - 근거: 요구사항 정의서 개정 이력, 2026-07-30 사용자 확정

3. 도서관 홈 화면 설계 — **완료** (2026-07-30)
   - 산출물: `frontend/screens/library-home.md` (9개 절, 와이어프레임 5개, API 호출 명세)
   - **설계 결정**: 관계 수 노출 (투명성 + 직관성, UD-28 기준) + 빈 상태 3가지 분류
   - **API 호출 검증**: 6개 호출 중 5개 명세 있음 ✅, 1개 갭 발견 → 갭 #21
   - **라이브러리 후보 제시**: `react-markdown` + `remark-gfm` (권고), `react-icons` (권고)
   - **갭 #21 신규 발견**: 모순 조회 API 부재 → backend에 전달, 리더에 보고
   - 근거: 기획서 16절, 요구사항 정의서 5.3절 (FR-KNW-08)

## 다음 작업

**1순위: 라이브러리 승인 대기**
- `react-markdown` + `remark-gfm`: Markdown 렌더링
- `react-icons`: 활동 타입 아이콘
- 리더 승인 후 컴포넌트 구현 시작

**2순위: 도서관 홈 컴포넌트 구현** (라이브러리 승인 후)
- 각 섹션 React 컴포넌트
- Skeleton loader (로딩 중)
- 에러 상태 처리
- 반응형 모바일 레이아웃 (기획서 16.5절)
- **목업 데이터로 먼저 완성, 파일명·주석에 목업 명시**
- **실 API 연동은 별도 작업** (헌법 제4조 6항)

**3순위: 변경 검토 화면 설계** (UD-06 확정)
- 4개 판단 버튼 (APPROVE, REJECT, REQUEST_CHANGES, DEFER)
- REQUEST_CHANGES: comment 필수 + 재생성 대기 UX
- 전후 diff: 클라이언트 계산 (UD-05 확정, Phase 1)
- 근거: 갭 #13, FR-CHG-05, API 명세 8.1절

**4순위: 원문 발췌 표기 설계** (UD-07·UD-24·UD-27 확정)
- 발췌 최대 길이: 400자 (UD-07 확정)
- 원문 언어 배지 (예: "[English]", "[한국어]")
- 회색 배경 또는 mono 글꼴 구분
- 원문 검색 하이라이트 포함 여부
- 근거: 갭 #14, FR-UIX-12·13

**5순위: Ingest 진행 화면 설계** (UD-02 고려)
- PAUSED_QUOTA vs FAILED 시각 구분 (갭 #15)
- polling 중 토큰 만료 처리 (갭 #20)
- 업로드 한도 사전 검증 (갭 #19)
- 근거: UD-02, FR-ING-26, FR-NTF-05

## 차단 요인

**컴포넌트 구현 대기:**
- 라이브러리 승인 (리더)
- 갭 #21 API 구현 (backend) — 선택사항 (도서관 홈 모션 모형은 갭 없이 진행 가능)

## 소유 산출물

| 경로 | 상태 | 갱신 |
| --- | --- | --- |
| `docs/검증-프론트엔드-화면-API-갭.md` | **완전 최신화** (갭 #2·#3 해소됨으로 정정, 갭 #19·#20·#21 추가) | 2026-07-30 |
| `.claude/team/frontend/STATE.md` | **현재 스냅샷** (도서관 홈 설계 완료, 라이브러리 승인 대기) | 2026-07-30 |
| `.claude/team/frontend/WORKLOG.md` | **2026-07-30 항목 append** | 2026-07-30 |
| `frontend/screens/library-home.md` | **설계 문서 (목록 + 와이어프레임 + API 명세)** | 2026-07-30 |
| `frontend/` | 생성됨 (screens 디렉터리 포함) | — |

## 루트 CLAUDE.md 업데이트 필요

프로젝트 구조 섹션:
- `frontend/` 상태: "미생성" → "생성됨" 필요 (리더 소유, 변경 요청)

## 다음 단계

**라이브러리 승인 후 컴포넌트 구현 (목업 데이터)**
1. 각 섹션 React 컴포넌트 (주요 주제·최근 발견·열린 질문·모순·검토 대기·최근 활동)
2. 목업 데이터 구조 정의
3. Skeleton + 에러 상태
4. 반응형 모바일 레이아웃
5. 실 API 연동은 별도 (갭 #21 API 완성 후)
