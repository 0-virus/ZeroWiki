# frontend 현재 상태

> **덮어쓰기 스냅샷.** 매 턴 종료 전에 이 파일 전체를 최신 상태로 갱신한다(헌법 제4조 1항).
> 시간순 기록은 `WORKLOG.md`에 append 한다.

마지막 갱신: 2026-07-29 23:59 (갭 문서 요약 헤더 수정, UD-28 권고안 검토)

## 현재 단계

**계약 확정 후 화면 설계 단계로 진입** — 2026-07-29 UD-04·UD-05 확정으로 갭 #8·#9 해소. 갭 #2·#3은 차단 요인 변경(UD-28으로 단순화). 신규 갭 6건 발견 중 갭 #16은 설계 결정 필요(미확정 아님). **코드 없음.**

## 진행 중 작업

1. UD-04·UD-05 확정 상태 검증 및 갭 문서 갱신 — **완료** (2026-07-29)
   - API 명세 18절 1490~1493행에 두 항목 확정 내용 명시
   - 요구사항 정의서 12절 687~688행에 취소선 처리됨
   - 갭 문서: 갭 #8·#9 "해소됨"으로 표기, 근거 절 번호 명시
   - 근거: frontend 권고 채택(2026-07-29, 리더)

2. 갭 #2·#3·#16 상태 정정 및 갭 문서 반영 — **완료** (2026-07-29)
   - 갭 #2: "형태 변경" (스키마는 개선, UD-28 미확정)
   - 갭 #3: "형태 변경" (기획서 16절 정의 명시, 구현 기준 UD-28)
   - 갭 #16: "설계 결정 필요" (UD-27 확정된 정책의 렌더링·검색 방식)
   - 각 갭에 근거 문서·절 번호 명시

3. 갭 문서 요약 헤더 수정 (고정 건수 제거) — **완료** (2026-07-29)
   - 2-A절 헤더: "해소된 갭 (0건)" → "해소된 갭", "여전히 남은 갭 (12건)" → "여전히 남은 갭"
   - 2-A절: "형태 변경된 갭" 섹션 신설
   - 2-B절 헤더: "새로 발견된 갭 (6건)" → "새로 발견된 갭"
   - 근거: 헌법 v1.2 제7조 8항 (고정 건수는 낡음, 목록을 직접 세어 계산)
   - 검증: `grep "([0-9]*건)" docs/검증-프론트엔드-화면-API-갭.md` = 결과 없음 ✅

## 다음 작업

1. **원본 언어와 무관하게 한국어 위키 본문 + 원문 발췌 혼합 화면 설계** — 갭 #14
   - Markdown 렌더링에서 발췌 구간 시각적 표기 방안 (언어 배지, 회색 배경 등)
   - 검색 하이라이트 처리 (원문 구간 포함 인덱싱)
   - 근거: UD-24·UD-27(확정), FR-UIX-12·13·14

2. **변경 검토 화면 4개 판단 버튼 + REQUEST_CHANGES 수정 요청 흐름** — 갭 #13
   - 기존 3개 → 4개 버튼 (REQUEST_CHANGES 추가)
   - comment 필수/선택 필드 규칙
   - 재생성 결과 대기 UX
   - 근거: FR-CHG-05, API 명세 8.1절

3. **처리량 보류(PAUSED_QUOTA) vs 실패(FAILED) 시각 구분** — 갭 #15
   - PAUSED_QUOTA: "보류 중, 구매 후 재개 가능"
   - FAILED: "종결, 재시도 필요"
   - 알림 통합 (FR-NTF-05)
   - 근거: FR-ING-26, FR-NTF-05

4. **원본 감지 언어 표시** — 갭 #17
   - 원본 목록에서 언어 배지 (예: "영어", "한국어")
   - 근거: API 5.1절 detectedLanguage

5. **원문 용어 병기(첫 등장 1회)의 검색·렌더링 전략** — 갭 #16
   - 전문 검색에 괄호 안 용어 포함 인덱싱
   - markdown 렌더링 스타일 (글꼴, 강조 방식)
   - diff 뷰에서 병기 추가/제거가 내용 변경으로 오인되지 않도록
   - 근거: UD-27(확정), FR-UIX-14

6. **기존 12개 갭의 상태·담당·차단 요인 정리** — 다음 단계 프로젝트
   - 갭 #1: **잔존** (API 명세 미반영) — backend 담당
   - 갭 #2: **형태 변경** (UD-28 미확정) — backend·기술 담당
   - 갭 #3: **형태 변경** (UD-28 미확정) — backend·기술 담당
   - 갭 #4, #5, #6, #11, #12: **잔존** (API 명세 미반영) — backend 담당
   - 갭 #7: **잔존** (설계 권고 필요) — frontend 권고
   - 갭 #8: **해소됨** (UD-05 확정) — Phase 1 스냅샷 + 클라이언트 계산
   - 갭 #9: **해소됨** (UD-04 확정) — Ingest 2초/AI 1초/Lint·Export 5초
   - 갭 #10: **잔존** (MVP 범위) — 리더 확정 필요 (Phase 2)

## 차단 요인

**화면 구현을 막는 미확정:**

1. **UD-28 (도서관 홈 주요 주제 산출 기준)** — 갭 #2·#3 차단
   - 기획서 16절: "AI가 콘텐츠를 분석해 관계도가 높은 주제 자동 선정"
   - pm 권고안 완성 (2026-07-29):
     - 주제 정의: `page_type='CONCEPT'` 페이지들 ✅
     - 측정 방법: 관계 수 (UNION ALL + COUNT) — Phase 1 즉시 구현 가능 ✅
     - 개수: LIMIT 6 (5~7 범위 중앙값) ✅
     - 재계산: Ingest 완료 시 ✅
   - 결정 주체: 기술(리더) — 확정 대기
   - 근거: `docs/PM-미확정결정-우선순위.md` 2.4절, 요구사항 정의서 12.2절
   - **Frontend 검토**: 화면 설계 차단 없음, 권고안으로 즉시 진행 가능

**설계 결정 필요 (미확정 아님):**

- 갭 #14: 원문 발췌 시각 표기 (언어 배지·배경색·글꼴 스타일)
- 갭 #16: 원문 용어 병기 렌더링·검색 (스타일·인덱싱·diff 표시)
- 갭 #7: 그래프 페이로드 형태 (노드·엣지 함께 vs 별도 조회)

## 소유 산출물

| 경로 | 상태 | 갱신 |
| --- | --- | --- |
| `docs/검증-프론트엔드-화면-API-갭.md` | **완전 최신화** (갭 상태 정정, 요약 헤더 수정, 고정 건수 제거) | 2026-07-29 |
| `.claude/team/frontend/STATE.md` | **최종 갱신** (현재 상태 스냅샷) | 2026-07-29 |
| `frontend/` | 미생성 (UD-28 확정 후 설계 시작) | — |
| 화면 와이어프레임 | 미작성 (갭 #14, #13, #15 우선) | — |

## 다음 단계

**UD-28 리더 확정 대기 중**

확정 시:
- 갭 #2·#3 형태 변경 → 해소 완료
- 도서관 홈 화면 설계 시작 가능 (갭 #14, #13, #15와 병행)
- 전체 화면 설계 시작 조건 구성

**현재 가능한 작업:**
- 갭 #14·#13·#15·#16·#17 설계 진행 가능 (UD-28 무관)
