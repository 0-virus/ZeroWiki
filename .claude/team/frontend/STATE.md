# frontend 현재 상태

> **덮어쓰기 스냅샷.** 매 턴 종료 전에 이 파일 전체를 최신 상태로 갱신한다(헌법 제4조 1항).
> 시간순 기록은 `WORKLOG.md`에 append 한다.

마지막 갱신: 2026-07-27 (리더가 스캐폴딩 생성 시 초기화)

## 현재 단계

Phase 0 이전 — 화면·API 갭 검증 완료, 계약 확정 대기. **코드 없음.**

## 진행 중 작업

없음.

## 다음 작업

1. 갭 12건을 소유자별로 분류해 전달 — backend(2, 4, 5, 6, 11, 12번), 리더(1, 3, 10번), 사용자 결정(8, 9번), frontend 권고(7번)
   근거: `docs/검증-프론트엔드-화면-API-갭.md` 2절
2. 미확정 계약 의견 제출 — API 명세 18절 5(diff 형식), 6(사용자 편집 안전 변경), 7(발췌문 길이)번 → qa에게 전달
3. 그래프 시각화 페이로드 형태 권고안 작성 (갭 #7)
4. 핵심 화면 6종 와이어프레임 (기획서 25절 5번 실행 과제)

## 차단 요인

- **사용자 결정 대기**: diff 표현 형식(API 18절 5번), polling 간격·최대 생성 시간(4번), 발췌문 최대 길이(7번)
- 갭 #1, #3, #10 — 리더의 API 명세 보강 없이는 글로벌 홈·도서관 홈 구현 불가
- 라이브러리 선정(Markdown 렌더러 / diff 뷰어 / 그래프) 미확정 → 리더 승인 필요

## 소유 산출물

| 경로 | 상태 |
| --- | --- |
| `docs/검증-프론트엔드-화면-API-갭.md` | 작성 완료 (갭 12건 열림) |
| `frontend/` | 미생성 |
| 와이어프레임 | 미작성 |
