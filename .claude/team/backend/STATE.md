# backend 현재 상태

> **덮어쓰기 스냅샷.** 매 턴 종료 전에 이 파일 전체를 최신 상태로 갱신한다(헌법 제4조 1항).
> 시간순 기록은 `WORKLOG.md`에 append 한다.

마지막 갱신: 2026-07-29 16:25 UTC (API 명세 리더 수정 반영 검증 + 미확정 목록 동기화 + UD-28 구현 검토 완료)

## 현재 단계

Phase 0 이전 — **문서 검증 및 스키마 조정 완료.** 요구사항 정의서(개정 이력 기준) 기반 ERD·API 명세 정합성 확인 완료. 리더 API 명세 수정 7건 반영 검증 완료. UD-28 구현 관점 검토 완료. **코드 없음.**

## 진행 중 작업

없음. ERD 및 API 명세 검증 완료.

## 완료 항목

**ERD 조정 (2026-07-29 완료):**
- ✓ revision 필드 (libraries, change_sets, library_constitution_versions)
- ✓ idempotency_records 엔터티 신규 추가 (멱등성 키 24시간 캐시)
- ✓ 복합 인덱스 16개 (API 필터·정렬·JOIN·검색)
- ✓ source_versions.detected_language 추가 (원본 언어 감지, API 2.8절·5.1절 계약 반영)
- ✓ idempotency_records.response_body 설명 수정 (서명 URL 제외, NFR-SEC-09 반영)
- ✓ ingest_jobs 상태 명확화 (8가지 + 부수 상태)
- ✓ ERD 8절을 UD-11~20과 상호 참조
- ✓ 헤딩 레벨 수정 (### 5.1)

**API 명세 리더 수정 7건 반영 검증 (2026-07-29 완료):**
- ✓ MAJOR 도서관 홈 응답: `topics` 필드 (API 명세 4.1절 395행·409행)
- ✓ MAJOR 배치 근거 조회: `GET /libraries/{libraryId}/pages/{pageId}/claims?include=evidences` (API 명세 7.2절 811행)
- ✓ MAJOR 상태 전이 명확화: 작업 상태 8종 + 운영 상태 2종 (API 명세 2.7절 219~239행)
- ✓ MINOR 페이지 topics: `"topics": []` 필드 (API 명세 7.1절 739행)
- ✓ MINOR 활동 집계: `GET /libraries/{libraryId}/activities/summary` (API 명세 11절 1246~1277행)
- ✓ MINOR 검색 확장: 결과의 `origin` 필드 및 `scope` (CURRENT/REFERENCED) (API 명세 9.1절 1050~1088행)
- ✓ ingest_jobs 상태 enum 확정 (API 명세 2.7절)

## 다음 작업

1. **확정된 미확정 항목 반영** (2026-07-29):
   - ~~UD-04~~ → **확정됨** (polling 간격: Retry-After 우선 + 기본값 Ingest 2초/AI 1초/Lint·Export 5초)
   - ~~UD-05~~ → **확정됨** (diff 표현: Phase 1 스냅샷 + 클라이언트 계산, Phase 2 서버 구조화 블록 diff)
   - ~~UD-24~~ → **확정됨** (위키 본문: 원본 언어와 무관하게 한국어로 종합)
   - ~~UD-27~~ → **확정됨** (원문 용어 병기: 초회 등장 1회 + 정의·핵심 주장 섹션 유지)

2. **신규 미확정 항목 추가:**
   - **UD-28** (도서관 홈 `topics`의 산출 기준) — 측정 방법·표시 개수·재계산 시점 미정, 기술 결정 (API 명세 4.1절 427행, 요구사항 정의서 12.2절)

3. **미확정 계약 의견 제출** — pm/qa와 협의:
   - ERD 8절 1, 2, 3, 5, 6, 7, 8번 (UD-11, 12, 13, 15, 16, 17, 18)
   - API 명세 18절 1, 2, 3, 6, 7, 8, 9, 10번 (UD-01, 02, 03, 06, 07, 08, 09, 10)
   - ChangeSet risk_level 판정 기준 권고안

## 차단 요인

**사용자 결정 대기** (기획서 1순위 기준, 요구사항 정의서 12절 정본):
- UD-01 (Import 최대 크기·문서 수)
- UD-02 (Token 수명)
- UD-03 (처리량 단위)
- UD-06 (사용자 편집 중 안전 변경 자동 적용)
- UD-07 (검색 결과 발췌문 최대 길이)
- UD-10 (Notion OAuth·결제 공급자)
- UD-14 (원본 발췌문 저작권·개인정보상 최대 범위)
- UD-19 (처리량 표시 단위·토큰 환산 규칙)
- UD-20 (감사 로그 보존 기간·관리자 접근 정책)
- UD-22 (신고 처리 주체·기한)

**기술 결정 대기** (리더 확정):
- UD-08 (공개 URL slug 충돌·변경 정책)
- UD-09 (관리자·운영 API 분리)
- UD-12 (페이지 버전별 Claim 복제 vs 재사용)
- UD-13 (도서관 버전 과거 조회: 이벤트 재생 vs 스냅샷)
- UD-15 (구조화 헌법 JSON Schema)
- UD-16 (관계 유형 초기 목록·사용자 정의 시점)
- UD-18 (Notion Import 외부 ID·중복 판정)
- UD-21 (월간 Lint 실행 시각·타임존·대상 선정)
- UD-23 (알림 보존 기간·읽음 처리 정책)
- UD-28 (도서관 홈 topics 산출 기준: 관계도 측정·표시 개수·재계산 시점)

**벤치마크 대기**:
- UD-11 (Claim 세밀도: 모든 문장 vs 중요 주장)
- UD-17 (pgvector 임베딩 모델·차원)
- UD-25 (성능 목표: 스캔 완료·답변 응답 시간)
- UD-26 (개별 승인 건수 상한)

## 소유 산출물

| 경로 | 상태 |
| --- | --- |
| `docs/ZeroWiki-ERD-초안.md` | v1.2 완성. 모든 검토 항목 반영 완료 |
| `docs/검증-백엔드-ERD-API-정합성.md` | 참조용 (수정 불필요) |
| `backend/` | 미생성 |

## 참고

- **요구사항 정의서:** 개정 이력의 마지막 행이 정본. 확정 4건(UD-04·05·24·27), 미확정 24건
- **API 명세:** 4.1절 topicSummary → topics 필드명 변경 반영됨
- **리더의 API 명세 수정 7건:** 미반영분 없음 (모두 반영됨)
- **미확정 분류 정본:** 요구사항 정의서 12절 (사용자 10건 + 기술 10건 + 벤치마크 4건)
- **UD-28 (도서관 홈 topics 산출) 구현 검토:**
  - ✅ 인덱스: page_relations의 필요 인덱스 이미 ERD 5.1절 763~764행에 정의
  - ✅ 상태 머신: CHANGE_REVIEW 단계 apply 직후 → 후속 Lint 전 재계산 가능, 충돌 없음
  - 권고: 배치는 변경 세트 apply 트랜잭션 커밋 후 비동기 실행, 초과 시간 시 백그라운드 연기
