import { ReviewChangesScreen } from '@/components/review-changes';

export const metadata = {
  title: 'Change Review',
  description: 'Review and approve/reject changes to your knowledge base',
};

export default function ReviewChangesPage() {
  return <ReviewChangesScreen />;
}
